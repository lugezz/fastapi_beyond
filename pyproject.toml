[project]
name = "fastapi-beyond"
version = "0.1.0"
description = "FastAPI Beyond CRUD Full Course - A FastAPI Course"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
]
